name: Setup

inputs:
  node-version:
    default: lts/*
    required: false

runs:
  using: 'composite'

  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: ${{ inputs.node-version }}
    - uses: oven-sh/setup-bun@v1

    - name: Cache bun deps
      uses: actions/cache@v3
      with:
        path: ~/.bun/install/cache
        key: ${{ runner.os }}-node${{ inputs.node-version }}-bun-${{ hashFiles('**/bun.lock') }}

    - name: Install deps
      run: bun install --frozen-lockfile
